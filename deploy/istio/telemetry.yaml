apiVersion: telemetry.istio.io/v1alpha1
kind: Telemetry
metadata: { name: payment-tracing, namespace: payments }
spec:
  tracing:
    - providers: [{ name: otel-tracing }]
      randomSamplingPercentage: 100.0
      customTags:
        experiment: { literal: { value: payment-mesh } }
